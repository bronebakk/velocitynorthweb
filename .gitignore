# Local tooling config — contains app passwords in approved-command rules. NEVER commit.
.claude/

# Build artifact of the legacy custom theme (not deployed)
velocity-north.zip
*.zip

# Personal data dropped into the project (CV / LinkedIn export) — never commit
Profile.pdf
*.pdf

# OS noise
.DS_Store

# Cowork / Obsidian draft files — local only
*.md
